# Git Workspace Setup Design

A comprehensive design for managing workspaces that may or may not have git initialized, and connecting them to remote repositories (GitHub, GitLab, Bitbucket).

## Table of Contents

1. [Problem Statement](#problem-statement)
2. [User Scenarios](#user-scenarios)
3. [Workspace States](#workspace-states)
4. [Git URL Parsing](#git-url-parsing)
5. [UI Design](#ui-design)
6. [Backend APIs](#backend-apis)
7. [Implementation](#implementation)

---

## Problem Statement

Currently, cdev-ios assumes all workspaces are git repositories. We need to:

1. **Detect** workspaces without git initialization
2. **Initialize** git for new projects
3. **Add remotes** by parsing git URLs (SSH/HTTPS)
4. **Set upstream** for tracking
5. **Provide seamless UX** for the entire setup flow

### Target User Flow

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   Create    │───▶│  Init Git   │───▶│ Add Remote  │───▶│    Push     │
│   Project   │    │  (1 tap)    │    │ (paste URL) │    │  (1 tap)    │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
     Local              Local            Connected          Synced
    Folder            Git Repo          to GitHub         to Remote
```

---

## User Scenarios

### Scenario 1: Brand New Project

```
Developer:
1. Creates new Xcode/VSCode project on Mac
2. Opens cdev-ios, connects to agent
3. Adds workspace → App detects "No Git"
4. Taps "Initialize Git" → git init + initial commit
5. Creates repo on GitHub (gets URL)
6. Taps "Add Remote" → pastes URL
7. Taps "Push" → code is on GitHub
```

### Scenario 2: Existing Git Repo, No Remote

```
Developer:
1. Has local git repo with commits
2. Decides to share on GitHub
3. Creates repo on GitHub
4. Opens cdev-ios → sees "No Remote"
5. Taps "Connect Remote" → pastes URL
6. Taps "Push" → synced
```

### Scenario 3: Clone from Remote (Future)

```
Developer:
1. Has GitHub repo URL
2. Opens cdev-ios
3. Taps "Clone Repository"
4. Pastes URL → agent clones
5. Workspace ready to use
```

---

## Workspace States

### State Machine

```
┌──────────────────────────────────────────────────────────────────┐
│                      WORKSPACE STATES                            │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│   ┌───────────┐                                                  │
│   │  NO_GIT   │  Folder without .git                            │
│   │    📁     │  Actions: [Initialize Git]                      │
│   └─────┬─────┘                                                  │
│         │ git init                                               │
│         ▼                                                        │
│   ┌───────────┐                                                  │
│   │ GIT_INIT  │  Git repo, no commits                           │
│   │    🌱     │  Actions: [Initial Commit]                      │
│   └─────┬─────┘                                                  │
│         │ git commit                                             │
│         ▼                                                        │
│   ┌───────────┐                                                  │
│   │ NO_REMOTE │  Git repo with commits, no remote               │
│   │    💻     │  Actions: [Add Remote]                          │
│   └─────┬─────┘                                                  │
│         │ git remote add                                         │
│         ▼                                                        │
│   ┌───────────┐                                                  │
│   │ NO_PUSH   │  Has remote, but never pushed                   │
│   │    🔗     │  Actions: [Push to Remote]                      │
│   └─────┬─────┘                                                  │
│         │ git push -u                                            │
│         ▼                                                        │
│   ┌───────────┐                                                  │
│   │  SYNCED   │  Fully configured and synced                    │
│   │    ✅     │  Actions: [Push] [Pull] [Fetch]                 │
│   └───────────┘                                                  │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

### State Detection Logic

```swift
enum WorkspaceGitState {
    case noGit          // No .git folder
    case gitInitialized // Has .git but no commits
    case noRemote       // Has commits but no remote
    case noPush         // Has remote but never pushed (no upstream)
    case synced         // Fully configured
    case diverged       // Has unpushed/unpulled commits
    case conflict       // Has merge conflicts

    var icon: String {
        switch self {
        case .noGit: return "folder"
        case .gitInitialized: return "leaf"
        case .noRemote: return "desktopcomputer"
        case .noPush: return "link"
        case .synced: return "checkmark.circle.fill"
        case .diverged: return "arrow.triangle.2.circlepath"
        case .conflict: return "exclamationmark.triangle.fill"
        }
    }

    var statusText: String {
        switch self {
        case .noGit: return "Not a Git Repository"
        case .gitInitialized: return "Git Initialized"
        case .noRemote: return "No Remote Configured"
        case .noPush: return "Ready to Push"
        case .synced: return "Synced"
        case .diverged: return "Changes to Sync"
        case .conflict: return "Conflicts"
        }
    }

    var actionRequired: String? {
        switch self {
        case .noGit: return "Initialize Git to start version control"
        case .gitInitialized: return "Make your first commit"
        case .noRemote: return "Add a remote to sync with GitHub/GitLab"
        case .noPush: return "Push to set up tracking"
        case .synced, .diverged, .conflict: return nil
        }
    }
}
```

---

## Git URL Parsing

### Supported URL Formats

| Provider | SSH Format | HTTPS Format |
|----------|------------|--------------|
| GitHub | `git@github.com:user/repo.git` | `https://github.com/user/repo.git` |
| GitLab | `git@gitlab.com:user/repo.git` | `https://gitlab.com/user/repo.git` |
| Bitbucket | `git@bitbucket.org:user/repo.git` | `https://bitbucket.org/user/repo.git` |
| Custom | `git@host:user/repo.git` | `https://host/user/repo.git` |

### URL Parser Implementation

```swift
// MARK: - Git URL Parser

struct GitRemoteURL {
    let originalURL: String
    let provider: GitProvider
    let host: String
    let owner: String
    let repoName: String
    let isSSH: Bool

    enum GitProvider: String, CaseIterable {
        case github = "github.com"
        case gitlab = "gitlab.com"
        case bitbucket = "bitbucket.org"
        case custom = "custom"

        var displayName: String {
            switch self {
            case .github: return "GitHub"
            case .gitlab: return "GitLab"
            case .bitbucket: return "Bitbucket"
            case .custom: return "Git Server"
            }
        }

        var icon: String {
            switch self {
            case .github: return "github.mark"  // SF Symbol or custom
            case .gitlab: return "g.square.fill"
            case .bitbucket: return "b.square.fill"
            case .custom: return "server.rack"
            }
        }

        var color: Color {
            switch self {
            case .github: return .primary
            case .gitlab: return .orange
            case .bitbucket: return .blue
            case .custom: return .gray
            }
        }
    }

    /// Parse a git URL string into components
    /// Supports both SSH and HTTPS formats
    static func parse(_ urlString: String) -> GitRemoteURL? {
        let trimmed = urlString.trimmingCharacters(in: .whitespacesAndNewlines)

        // SSH format: git@github.com:username/repo.git
        if trimmed.hasPrefix("git@") {
            return parseSSH(trimmed)
        }

        // HTTPS format: https://github.com/username/repo.git
        if trimmed.hasPrefix("https://") || trimmed.hasPrefix("http://") {
            return parseHTTPS(trimmed)
        }

        // Try to auto-detect and fix common mistakes
        // e.g., "github.com/user/repo" without protocol
        if trimmed.contains("/") && !trimmed.contains("@") {
            return parseHTTPS("https://\(trimmed)")
        }

        return nil
    }

    private static func parseSSH(_ url: String) -> GitRemoteURL? {
        // Pattern: git@host:owner/repo.git
        // Example: git@github.com:brianly1003/Cdev.git

        let pattern = #"^git@([^:]+):([^/]+)/(.+?)(?:\.git)?$"#
        guard let regex = try? NSRegularExpression(pattern: pattern),
              let match = regex.firstMatch(in: url, range: NSRange(url.startIndex..., in: url)),
              match.numberOfRanges == 4 else {
            return nil
        }

        let hostRange = Range(match.range(at: 1), in: url)!
        let ownerRange = Range(match.range(at: 2), in: url)!
        let repoRange = Range(match.range(at: 3), in: url)!

        let host = String(url[hostRange])
        let owner = String(url[ownerRange])
        let repo = String(url[repoRange])

        let provider = GitProvider.allCases.first { $0.rawValue == host } ?? .custom

        return GitRemoteURL(
            originalURL: url,
            provider: provider,
            host: host,
            owner: owner,
            repoName: repo,
            isSSH: true
        )
    }

    private static func parseHTTPS(_ url: String) -> GitRemoteURL? {
        // Pattern: https://host/owner/repo.git
        // Example: https://github.com/brianly1003/Cdev.git

        guard let urlObj = URL(string: url),
              let host = urlObj.host else {
            return nil
        }

        let pathComponents = urlObj.pathComponents.filter { $0 != "/" }
        guard pathComponents.count >= 2 else { return nil }

        let owner = pathComponents[0]
        var repo = pathComponents[1]

        // Remove .git suffix if present
        if repo.hasSuffix(".git") {
            repo = String(repo.dropLast(4))
        }

        let provider = GitProvider.allCases.first { $0.rawValue == host } ?? .custom

        return GitRemoteURL(
            originalURL: url,
            provider: provider,
            host: host,
            owner: owner,
            repoName: repo,
            isSSH: false
        )
    }

    /// Convert to the other format (SSH ↔ HTTPS)
    var alternateURL: String {
        if isSSH {
            return "https://\(host)/\(owner)/\(repoName).git"
        } else {
            return "git@\(host):\(owner)/\(repoName).git"
        }
    }

    /// Display name for UI
    var displayName: String {
        "\(owner)/\(repoName)"
    }

    /// Full URL for git commands
    var fullURL: String {
        originalURL.hasSuffix(".git") ? originalURL : "\(originalURL).git"
    }
}
```

---

## UI Design

### 1. Workspace Card (Enhanced)

Show git state prominently on workspace cards:

```
┌─────────────────────────────────────────────────────────────────┐
│  WORKSPACE CARD - Normal State (Synced)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 📁 MyProject                                    ✅ Synced│   │
│  │    ~/Projects/MyProject                                  │   │
│  │    ────────────────────────────────────────────────────  │   │
│  │    🔀 main   ↑2 ↓0   │  GitHub: brianly1003/MyProject   │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  WORKSPACE CARD - Setup Required (No Git)                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 📁 NewProject                              ⚠️ No Git    │   │
│  │    ~/Projects/NewProject                                 │   │
│  │    ────────────────────────────────────────────────────  │   │
│  │    ┌──────────────────────────────────────────────────┐ │   │
│  │    │  🌱 Initialize Git to start version control      │ │   │
│  │    │                          [Initialize Git]        │ │   │
│  │    └──────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  WORKSPACE CARD - Setup Required (No Remote)                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 📁 LocalProject                           💻 No Remote  │   │
│  │    ~/Projects/LocalProject                               │   │
│  │    ────────────────────────────────────────────────────  │   │
│  │    🔀 main   (local only)                                │   │
│  │    ┌──────────────────────────────────────────────────┐ │   │
│  │    │  🔗 Add a remote to sync with GitHub/GitLab      │ │   │
│  │    │                            [Connect Remote]      │ │   │
│  │    └──────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2. Git Setup Wizard (Sheet)

A step-by-step wizard for setting up git:

```
┌─────────────────────────────────────────────────────────────────┐
│  Git Setup                                              ✕ Close │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  MyProject                                                      │
│  ~/Projects/MyProject                                           │
│                                                                 │
│  ─────────────────────────────────────────────────────────────  │
│                                                                 │
│  SETUP PROGRESS                                                 │
│                                                                 │
│  ✅ ─────● Initialize Git                                       │
│           Git repository initialized                            │
│                                                                 │
│  ✅ ─────● Initial Commit                                       │
│           Created initial commit with 12 files                  │
│                                                                 │
│  🔵 ─────● Add Remote                          ← Current Step   │
│           Connect to GitHub, GitLab, or other                   │
│                                                                 │
│  ○ ──────○ Push to Remote                                       │
│           Sync your code to the cloud                           │
│                                                                 │
│  ─────────────────────────────────────────────────────────────  │
│                                                                 │
│  ADD REMOTE REPOSITORY                                          │
│                                                                 │
│  Paste your repository URL:                                     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ git@github.com:brianly1003/MyProject.git            📋  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  ✓ Valid GitHub URL detected                            │   │
│  │                                                         │   │
│  │  🐙 GitHub                                              │   │
│  │  📦 brianly1003/MyProject                               │   │
│  │  🔐 SSH connection                                       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  Remote name: ┌────────────────┐                                │
│               │ origin         │ (default)                      │
│               └────────────────┘                                │
│                                                                 │
│                                        [Skip]  [Add Remote]     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3. Quick Add Remote (Compact)

For users who just want to add a remote quickly:

```
┌─────────────────────────────────────────────────────────────────┐
│  Add Remote                                             ✕ Close │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Paste repository URL                                           │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ https://github.com/brianly1003/Cdev.git             📋  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ───────────── OR ─────────────                                 │
│                                                                 │
│  Quick connect:                                                 │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐                   │
│  │  GitHub   │  │  GitLab   │  │ Bitbucket │                   │
│  │    🐙     │  │    🦊     │  │    🪣     │                   │
│  └───────────┘  └───────────┘  └───────────┘                   │
│                                                                 │
│  ─────────────────────────────────────────────────────────────  │
│                                                                 │
│  Preview:                                                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Provider:  GitHub                                       │   │
│  │ Owner:     brianly1003                                  │   │
│  │ Repo:      Cdev                                         │   │
│  │ Protocol:  HTTPS                        [Switch to SSH] │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│                                              [Add Remote]       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4. Remote URL Input with Live Validation

```
┌─────────────────────────────────────────────────────────────────┐
│  REMOTE URL INPUT - Empty State                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Repository URL                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Paste SSH or HTTPS URL...                           📋  │   │
│  └─────────────────────────────────────────────────────────┘   │
│  Supports GitHub, GitLab, Bitbucket, and custom git servers    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  REMOTE URL INPUT - Valid SSH URL                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Repository URL                                       ✓ Valid   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ git@github.com:brianly1003/Cdev.git                 ✕   │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 🐙  GitHub  ·  brianly1003/Cdev  ·  SSH                 │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  REMOTE URL INPUT - Invalid URL                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Repository URL                                       ✗ Invalid │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ github.com/brianly1003                              ✕   │   │
│  └─────────────────────────────────────────────────────────┘   │
│  ⚠️ Invalid URL format. Use SSH (git@...) or HTTPS (https://...)│
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 5. Workspace List with Git States

Show all workspaces with their git status at a glance:

```
┌─────────────────────────────────────────────────────────────────┐
│  Workspaces                                        + Add New    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  🔍 Search workspaces...                                        │
│                                                                 │
│  ─────────────────────────────────────────────────────────────  │
│                                                                 │
│  ACTIVE                                                         │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ cdev-ios                                    ✅ Synced    │   │
│  │ ~/Projects/cdev-ios                                      │   │
│  │ 🔀 main ↑0 ↓0  │  🐙 brianly1003/cdev-ios              │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ─────────────────────────────────────────────────────────────  │
│                                                                 │
│  NEEDS SETUP                                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ NewMobileApp                                📁 No Git   │   │
│  │ ~/Projects/NewMobileApp                                  │   │
│  │                              [Setup Git →]               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ ExperimentProject                         💻 No Remote  │   │
│  │ ~/Projects/ExperimentProject                             │   │
│  │ 🔀 main (local)               [Connect Remote →]         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ─────────────────────────────────────────────────────────────  │
│                                                                 │
│  READY TO SYNC                                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ ClientProject                              🔗 Not Pushed│   │
│  │ ~/Projects/ClientProject                                 │   │
│  │ 🔀 main  │  🐙 client/project       [Push to Remote →]  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 6. Branch Header with Remote Info

Enhanced branch header showing remote status:

```
┌─────────────────────────────────────────────────────────────────┐
│  BRANCH HEADER - With Remote                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 🔀 main ▼  →  🐙 origin/main     ↓ Pull  ↑ Push  🔄    │   │
│  │             ↑2 ahead  ↓0 behind                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  BRANCH HEADER - No Remote (Needs Setup)                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 🔀 main ▼  →  ⚠️ No remote         [+ Add Remote]  🔄   │   │
│  │             local only                                   │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  BRANCH HEADER - No Git (Needs Init)                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 📁 Not a git repository          [Initialize Git]  🔄   │   │
│  │    Setup version control to track changes               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 7. Remote Management View

For viewing and managing remotes:

```
┌─────────────────────────────────────────────────────────────────┐
│  Remotes                                            + Add New   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ origin                                        ★ Default │   │
│  │ 🐙 GitHub · brianly1003/cdev-ios                        │   │
│  │                                                         │   │
│  │ Fetch: git@github.com:brianly1003/cdev-ios.git         │   │
│  │ Push:  git@github.com:brianly1003/cdev-ios.git         │   │
│  │                                                         │   │
│  │ Branches tracking this remote:                          │   │
│  │ • main → origin/main                                    │   │
│  │ • develop → origin/develop                              │   │
│  │                                                         │   │
│  │                      [Fetch]  [Edit]  [Remove]          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ upstream                                                │   │
│  │ 🐙 GitHub · original-author/cdev-ios                    │   │
│  │                                                         │   │
│  │ Fetch: https://github.com/original-author/cdev-ios.git │   │
│  │ Push:  (none - fetch only)                              │   │
│  │                                                         │   │
│  │                      [Fetch]  [Edit]  [Remove]          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Backend APIs

### Check Git Status (Enhanced)

```json
// Request
{
  "method": "workspace/git/status",
  "params": {
    "workspace_id": "uuid"
  }
}

// Response (Enhanced)
{
  "is_git_repo": true,
  "has_commits": true,
  "branch": "main",
  "upstream": "origin/main",
  "ahead": 2,
  "behind": 0,
  "remotes": [
    {
      "name": "origin",
      "fetch_url": "git@github.com:brianly1003/cdev-ios.git",
      "push_url": "git@github.com:brianly1003/cdev-ios.git",
      "provider": "github"
    }
  ],
  "staged": [...],
  "unstaged": [...],
  "untracked": [...],
  "state": "synced"  // noGit, gitInit, noRemote, noPush, synced, diverged, conflict
}

// Response (No Git)
{
  "is_git_repo": false,
  "has_commits": false,
  "state": "noGit",
  "suggested_actions": ["init"]
}
```

### Initialize Git

```json
// Request
{
  "method": "workspace/git/init",
  "params": {
    "workspace_id": "uuid",
    "initial_branch": "main",
    "initial_commit": true,
    "commit_message": "Initial commit"
  }
}

// Response
{
  "success": true,
  "branch": "main",
  "commit_sha": "abc123...",
  "files_committed": 12
}
```

### Add Remote

```json
// Request
{
  "method": "workspace/git/remote/add",
  "params": {
    "workspace_id": "uuid",
    "name": "origin",
    "url": "git@github.com:brianly1003/cdev-ios.git",
    "fetch": true  // Also fetch after adding
  }
}

// Response
{
  "success": true,
  "remote": {
    "name": "origin",
    "fetch_url": "git@github.com:brianly1003/cdev-ios.git",
    "push_url": "git@github.com:brianly1003/cdev-ios.git",
    "provider": "github"
  },
  "fetched_branches": ["main", "develop"]
}
```

### Remove Remote

```json
// Request
{
  "method": "workspace/git/remote/remove",
  "params": {
    "workspace_id": "uuid",
    "name": "origin"
  }
}

// Response
{
  "success": true
}
```

### List Remotes

```json
// Request
{
  "method": "workspace/git/remote/list",
  "params": {
    "workspace_id": "uuid"
  }
}

// Response
{
  "remotes": [
    {
      "name": "origin",
      "fetch_url": "git@github.com:brianly1003/cdev-ios.git",
      "push_url": "git@github.com:brianly1003/cdev-ios.git",
      "provider": "github",
      "tracking_branches": ["main", "develop"]
    }
  ]
}
```

### Set Upstream

```json
// Request
{
  "method": "workspace/git/branch/set-upstream",
  "params": {
    "workspace_id": "uuid",
    "branch": "main",
    "upstream": "origin/main"
  }
}

// Response
{
  "success": true,
  "branch": "main",
  "upstream": "origin/main"
}
```

---

## Implementation

### SwiftUI Components

#### Git State Badge

```swift
struct GitStateBadge: View {
    let state: WorkspaceGitState

    var body: some View {
        HStack(spacing: 4) {
            Image(systemName: state.icon)
                .font(.system(size: 10))
            Text(state.statusText)
                .font(Typography.badge)
        }
        .foregroundStyle(stateColor)
        .padding(.horizontal, 8)
        .padding(.vertical, 4)
        .background(stateColor.opacity(0.15))
        .clipShape(Capsule())
    }

    private var stateColor: Color {
        switch state {
        case .noGit, .gitInitialized, .noRemote, .noPush:
            return ColorSystem.warning
        case .synced:
            return ColorSystem.success
        case .diverged:
            return ColorSystem.info
        case .conflict:
            return ColorSystem.error
        }
    }
}
```

#### Remote URL Input Field

```swift
struct RemoteURLInputField: View {
    @Binding var urlString: String
    @State private var parsedURL: GitRemoteURL?
    @State private var isValid: Bool = false

    var body: some View {
        VStack(alignment: .leading, spacing: Spacing.xs) {
            // Input field
            HStack {
                TextField("Paste SSH or HTTPS URL...", text: $urlString)
                    .font(Typography.terminal)
                    .autocapitalization(.none)
                    .disableAutocorrection(true)
                    .onChange(of: urlString) { _, newValue in
                        validateURL(newValue)
                    }

                // Paste button
                Button {
                    if let clipboard = UIPasteboard.general.string {
                        urlString = clipboard
                    }
                } label: {
                    Image(systemName: "doc.on.clipboard")
                        .font(.system(size: 16))
                        .foregroundStyle(ColorSystem.textSecondary)
                }

                // Clear button
                if !urlString.isEmpty {
                    Button {
                        urlString = ""
                        parsedURL = nil
                    } label: {
                        Image(systemName: "xmark.circle.fill")
                            .font(.system(size: 16))
                            .foregroundStyle(ColorSystem.textTertiary)
                    }
                }

                // Validation indicator
                if !urlString.isEmpty {
                    Image(systemName: isValid ? "checkmark.circle.fill" : "xmark.circle.fill")
                        .font(.system(size: 16))
                        .foregroundStyle(isValid ? ColorSystem.success : ColorSystem.error)
                }
            }
            .padding(Spacing.sm)
            .background(ColorSystem.terminalBgElevated)
            .clipShape(RoundedRectangle(cornerRadius: 8))

            // Parsed URL preview
            if let parsed = parsedURL {
                ParsedURLPreview(url: parsed)
            } else if !urlString.isEmpty && !isValid {
                Text("Invalid URL format. Use SSH (git@...) or HTTPS (https://...)")
                    .font(Typography.terminalSmall)
                    .foregroundStyle(ColorSystem.error)
            }
        }
    }

    private func validateURL(_ value: String) {
        parsedURL = GitRemoteURL.parse(value)
        isValid = parsedURL != nil
    }
}

struct ParsedURLPreview: View {
    let url: GitRemoteURL

    var body: some View {
        HStack(spacing: Spacing.sm) {
            // Provider icon
            providerIcon
                .font(.system(size: 20))
                .foregroundStyle(url.provider.color)

            VStack(alignment: .leading, spacing: 2) {
                Text(url.provider.displayName)
                    .font(Typography.terminal)
                    .foregroundStyle(ColorSystem.textPrimary)

                Text(url.displayName)
                    .font(Typography.terminalSmall)
                    .foregroundStyle(ColorSystem.textSecondary)
            }

            Spacer()

            // Protocol badge
            Text(url.isSSH ? "SSH" : "HTTPS")
                .font(Typography.badge)
                .foregroundStyle(ColorSystem.textSecondary)
                .padding(.horizontal, 6)
                .padding(.vertical, 2)
                .background(ColorSystem.textSecondary.opacity(0.15))
                .clipShape(Capsule())
        }
        .padding(Spacing.sm)
        .background(ColorSystem.success.opacity(0.1))
        .clipShape(RoundedRectangle(cornerRadius: 8))
    }

    @ViewBuilder
    private var providerIcon: some View {
        switch url.provider {
        case .github:
            Image("github-mark")  // Custom asset
        case .gitlab:
            Image(systemName: "g.square.fill")
        case .bitbucket:
            Image(systemName: "b.square.fill")
        case .custom:
            Image(systemName: "server.rack")
        }
    }
}
```

#### Git Setup Wizard

```swift
struct GitSetupWizard: View {
    @ObservedObject var viewModel: GitSetupViewModel
    @Environment(\.dismiss) private var dismiss

    var body: some View {
        NavigationStack {
            VStack(spacing: 0) {
                // Progress steps
                SetupProgressView(
                    steps: viewModel.steps,
                    currentStep: viewModel.currentStep
                )
                .padding()

                Divider()

                // Current step content
                ScrollView {
                    currentStepContent
                        .padding()
                }

                Divider()

                // Actions
                HStack {
                    if viewModel.canGoBack {
                        Button("Back") {
                            viewModel.goBack()
                        }
                        .buttonStyle(.bordered)
                    }

                    Spacer()

                    if viewModel.canSkip {
                        Button("Skip") {
                            viewModel.skip()
                        }
                        .foregroundStyle(ColorSystem.textSecondary)
                    }

                    Button(viewModel.isLastStep ? "Done" : "Continue") {
                        if viewModel.isLastStep {
                            dismiss()
                        } else {
                            Task { await viewModel.continueToNext() }
                        }
                    }
                    .buttonStyle(.borderedProminent)
                    .disabled(!viewModel.canContinue)
                }
                .padding()
            }
            .navigationTitle("Git Setup")
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
                ToolbarItem(placement: .cancellationAction) {
                    Button("Cancel") { dismiss() }
                }
            }
        }
    }

    @ViewBuilder
    private var currentStepContent: some View {
        switch viewModel.currentStep {
        case .initGit:
            InitGitStepView(viewModel: viewModel)
        case .initialCommit:
            InitialCommitStepView(viewModel: viewModel)
        case .addRemote:
            AddRemoteStepView(viewModel: viewModel)
        case .push:
            PushStepView(viewModel: viewModel)
        case .complete:
            CompleteStepView()
        }
    }
}

struct SetupProgressView: View {
    let steps: [SetupStep]
    let currentStep: SetupStep

    var body: some View {
        HStack(spacing: 0) {
            ForEach(Array(steps.enumerated()), id: \.element) { index, step in
                // Step indicator
                VStack(spacing: 4) {
                    ZStack {
                        Circle()
                            .fill(stepColor(for: step))
                            .frame(width: 24, height: 24)

                        if step < currentStep {
                            Image(systemName: "checkmark")
                                .font(.system(size: 12, weight: .bold))
                                .foregroundStyle(.white)
                        } else {
                            Text("\(index + 1)")
                                .font(.system(size: 12, weight: .bold))
                                .foregroundStyle(step == currentStep ? .white : ColorSystem.textTertiary)
                        }
                    }

                    Text(step.title)
                        .font(Typography.terminalSmall)
                        .foregroundStyle(step <= currentStep ? ColorSystem.textPrimary : ColorSystem.textTertiary)
                }

                // Connector line
                if index < steps.count - 1 {
                    Rectangle()
                        .fill(step < currentStep ? ColorSystem.success : ColorSystem.textQuaternary)
                        .frame(height: 2)
                        .frame(maxWidth: .infinity)
                        .padding(.horizontal, 4)
                        .offset(y: -10)
                }
            }
        }
    }

    private func stepColor(for step: SetupStep) -> Color {
        if step < currentStep {
            return ColorSystem.success
        } else if step == currentStep {
            return ColorSystem.primary
        } else {
            return ColorSystem.textQuaternary
        }
    }
}

enum SetupStep: Int, Comparable, CaseIterable {
    case initGit
    case initialCommit
    case addRemote
    case push
    case complete

    var title: String {
        switch self {
        case .initGit: return "Init"
        case .initialCommit: return "Commit"
        case .addRemote: return "Remote"
        case .push: return "Push"
        case .complete: return "Done"
        }
    }

    static func < (lhs: SetupStep, rhs: SetupStep) -> Bool {
        lhs.rawValue < rhs.rawValue
    }
}
```

### ViewModel

```swift
@MainActor
final class GitSetupViewModel: ObservableObject {
    // MARK: - Published State

    @Published var currentStep: SetupStep = .initGit
    @Published var isLoading: Bool = false
    @Published var error: String?

    // Step-specific state
    @Published var remoteURL: String = ""
    @Published var remoteName: String = "origin"
    @Published var commitMessage: String = "Initial commit"
    @Published var parsedRemoteURL: GitRemoteURL?

    // Completed steps tracking
    @Published var completedSteps: Set<SetupStep> = []

    // MARK: - Dependencies

    private let workspaceManager: WorkspaceManagerService
    private let workspaceId: String

    // MARK: - Computed Properties

    var steps: [SetupStep] {
        // Determine which steps are needed based on workspace state
        // This could be dynamic based on what's already done
        [.initGit, .initialCommit, .addRemote, .push, .complete]
    }

    var canGoBack: Bool {
        currentStep > .initGit && currentStep != .complete
    }

    var canSkip: Bool {
        currentStep == .addRemote  // Remote is optional
    }

    var canContinue: Bool {
        switch currentStep {
        case .initGit:
            return true  // Always can init
        case .initialCommit:
            return !commitMessage.isEmpty
        case .addRemote:
            return parsedRemoteURL != nil || canSkip
        case .push:
            return true
        case .complete:
            return true
        }
    }

    var isLastStep: Bool {
        currentStep == .complete
    }

    // MARK: - Init

    init(workspaceId: String, workspaceManager: WorkspaceManagerService = .shared) {
        self.workspaceId = workspaceId
        self.workspaceManager = workspaceManager
    }

    // MARK: - Actions

    func continueToNext() async {
        guard canContinue else { return }

        isLoading = true
        error = nil

        do {
            switch currentStep {
            case .initGit:
                try await initializeGit()
            case .initialCommit:
                try await createInitialCommit()
            case .addRemote:
                if let url = parsedRemoteURL {
                    try await addRemote(url: url)
                }
            case .push:
                try await pushToRemote()
            case .complete:
                break
            }

            completedSteps.insert(currentStep)
            moveToNextStep()
        } catch {
            self.error = error.localizedDescription
        }

        isLoading = false
    }

    func goBack() {
        guard canGoBack else { return }
        if let currentIndex = steps.firstIndex(of: currentStep), currentIndex > 0 {
            currentStep = steps[currentIndex - 1]
        }
    }

    func skip() {
        guard canSkip else { return }
        moveToNextStep()
    }

    // MARK: - Private Methods

    private func moveToNextStep() {
        if let currentIndex = steps.firstIndex(of: currentStep),
           currentIndex < steps.count - 1 {
            currentStep = steps[currentIndex + 1]
        }
    }

    private func initializeGit() async throws {
        let result = try await workspaceManager.gitInit(
            workspaceId: workspaceId,
            initialBranch: "main"
        )
        guard result.success else {
            throw GitSetupError.initFailed(result.error ?? "Unknown error")
        }
    }

    private func createInitialCommit() async throws {
        // Stage all files first
        _ = try await workspaceManager.gitStage(workspaceId: workspaceId, paths: ["."])

        // Create commit
        let result = try await workspaceManager.gitCommit(
            workspaceId: workspaceId,
            message: commitMessage,
            push: false
        )
        guard result.success else {
            throw GitSetupError.commitFailed(result.error ?? "Unknown error")
        }
    }

    private func addRemote(url: GitRemoteURL) async throws {
        let result = try await workspaceManager.gitRemoteAdd(
            workspaceId: workspaceId,
            name: remoteName,
            url: url.fullURL
        )
        guard result.success else {
            throw GitSetupError.remoteAddFailed(result.error ?? "Unknown error")
        }
    }

    private func pushToRemote() async throws {
        let result = try await workspaceManager.gitPush(
            workspaceId: workspaceId,
            force: false,
            setUpstream: true
        )
        guard result.isSuccess else {
            throw GitSetupError.pushFailed(result.message ?? "Unknown error")
        }
    }
}

enum GitSetupError: LocalizedError {
    case initFailed(String)
    case commitFailed(String)
    case remoteAddFailed(String)
    case pushFailed(String)

    var errorDescription: String? {
        switch self {
        case .initFailed(let msg): return "Failed to initialize git: \(msg)"
        case .commitFailed(let msg): return "Failed to create commit: \(msg)"
        case .remoteAddFailed(let msg): return "Failed to add remote: \(msg)"
        case .pushFailed(let msg): return "Failed to push: \(msg)"
        }
    }
}
```

---

## Summary

### Key Features

1. **State Detection**: Automatically detect workspace git state
2. **Setup Wizard**: Step-by-step guide for new projects
3. **URL Parsing**: Smart parsing of SSH/HTTPS URLs
4. **Provider Detection**: Recognize GitHub, GitLab, Bitbucket
5. **Quick Actions**: One-tap solutions for common tasks
6. **Remote Management**: View and manage multiple remotes

### Backend APIs Needed

| Method | Priority |
|--------|----------|
| `workspace/git/init` | HIGH |
| `workspace/git/remote/add` | HIGH |
| `workspace/git/remote/list` | HIGH |
| `workspace/git/remote/remove` | MEDIUM |
| `workspace/git/branch/set-upstream` | MEDIUM |

### UI Components to Build

| Component | Priority |
|-----------|----------|
| `GitStateBadge` | HIGH |
| `RemoteURLInputField` | HIGH |
| `GitSetupWizard` | HIGH |
| `WorkspaceGitStatusView` | HIGH |
| `RemoteManagementView` | MEDIUM |
| `ParsedURLPreview` | HIGH |

### Mobile UX Principles

1. **Progressive Disclosure**: Show only what's needed at each step
2. **Smart Defaults**: Pre-fill sensible values (origin, main, etc.)
3. **Instant Validation**: Validate URLs as user types
4. **Clear Feedback**: Show exactly what will happen
5. **Recovery Options**: Allow skip, back, and retry
6. **Clipboard Integration**: One-tap paste for URLs
